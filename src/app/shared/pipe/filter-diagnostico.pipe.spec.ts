import { FilterDiagnosticoPipe } from './filter-diagnostico.pipe';

describe('FilterDiagnosticoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDiagnosticoPipe();
    expect(pipe).toBeTruthy();
  });
});
