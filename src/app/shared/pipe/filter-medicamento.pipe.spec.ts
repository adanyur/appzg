import { FilterMedicamentoPipe } from './filter-medicamento.pipe';

describe('FilterMedicamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMedicamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
